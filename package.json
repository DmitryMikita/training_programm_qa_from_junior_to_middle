{
  "name": "qa-training-program",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": "node server.js",
    "cms:dev": "npm --prefix cms run develop",
    "cms:build": "npm --prefix cms run build"
  }
}
